% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_water_balance.R
\name{calc_water_balance}
\alias{calc_water_balance}
\title{Calculate Daily Soil Water Balance}
\usage{
calc_water_balance(
  et0,
  rainfall,
  daily_kc_values,
  root_depth,
  theta_fc,
  theta_wp,
  depletion_factor,
  initial_depletion = 0,
  day_numbers = NULL,
  irrigation_rule = "threshold"
)
}
\arguments{
\item{et0}{Numeric vector. Daily reference evapotranspiration (mm/day).}

\item{rainfall}{Numeric vector. Daily rainfall (mm/day).}

\item{daily_kc_values}{Numeric vector. Daily crop coefficient (Kc) values.}

\item{root_depth}{Numeric vector or single value. Daily root depth (mm). If a single value is provided, it applies to all days.}

\item{theta_fc}{Numeric. Volumetric soil water content at field capacity (m³/m³).}

\item{theta_wp}{Numeric. Volumetric soil water content at wilting point (m³/m³).}

\item{depletion_factor}{Numeric. The fraction of Total Available Water (TAW) that a crop can extract from the root zone without suffering water stress (the 'p' factor from FAO-56). Must be between 0 and 1.}

\item{initial_depletion}{Numeric. Initial soil water depletion at the start of day 1 (mm). Defaults to 0 (field capacity).}

\item{day_numbers}{Numeric vector (optional). Sequence of days (e.g., day of the year). Defaults to a sequence from 1 to the length of \code{daily_kc_values}.}

\item{irrigation_rule}{Character or Integer. Defines the irrigation trigger. Use \code{"threshold"} (default) to irrigate when depletion exceeds Readily Available Water (RAW), or a positive integer \code{N} to irrigate at a fixed N-day interval.}
}
\value{
A list containing:
\item{water_balance_data}{A data.frame with the detailed daily soil water balance components:
\code{day}: Day number.
\code{rainfall}: Input rainfall (mm).
\code{et0}: Input reference ET0 (mm).
\code{root_depth}: Root depth for the day (mm).
\code{taw}: Total Available Water in the root zone (mm).
\code{raw}: Readily Available Water in the root zone (mm).
\code{depletion_start}: Soil water depletion at the start of the day (mm).
\code{kc}: Crop coefficient for the day.
\code{ks}: Water stress coefficient (0-1).
\code{etc}: Actual crop evapotranspiration (mm).
\code{depletion_end}: Soil water depletion at the end of the day before irrigation (mm).
\code{irrigation_applied}: Irrigation depth applied (mm).
\code{water_surplus}: Water surplus (deep percolation or runoff) (mm).
}
\item{summary_depths}{A list with cumulative water balance components:
\code{total_rainfall}: Total input rainfall (mm).
\code{total_water_surplus}: Total water surplus (mm).
\code{net_rainfall}: Total rainfall minus total water surplus (mm).
\code{total_etc}: Total actual crop evapotranspiration (mm).
\code{total_irrigation_applied}: Total irrigation depth applied (mm).
\code{irrigation_events_count}: Number of irrigation events.
}
}
\description{
Calculates the daily soil water balance for a crop over a specified period.
The function accounts for reference evapotranspiration, rainfall, crop coefficients,
soil properties, and irrigation rules to estimate actual crop evapotranspiration (ETc),
soil water depletion, irrigation needs, and water surplus.
}
\examples{
# For a standalone example, let's create a dummy daily_kc_values
daily_kc_lettuce <- c(
  rep(0.7, 15),
  seq(0.7, 1.05, length.out = 20),
  rep(1.05, 20),
  seq(1.05, 0.9, length.out = 10)
) # Total 65 days

set.seed(123) # for reproducibility
sim_days <- length(daily_kc_lettuce)
et0_data <- round(runif(sim_days, 2, 5), 1)
rain_data <- round(sample(c(rep(0, sim_days - 5), runif(5, 3, 30))), 0)

root_depth_val <- 300 # mm
p_factor_val <- 0.55
theta_fc_val <- 0.30 # m3/m3
theta_wp_val <- 0.15 # m3/m3

water_balance_results <- calc_water_balance(
  et0 = et0_data,
  rainfall = rain_data,
  daily_kc_values = daily_kc_lettuce,
  root_depth = root_depth_val,
  theta_fc = theta_fc_val,
  theta_wp = theta_wp_val,
  depletion_factor = p_factor_val,
  initial_depletion = 0, # Starting at field capacity
  irrigation_rule = "threshold"
)

print(head(water_balance_results$water_balance_data))
print(water_balance_results$summary_depths)

# Example with fixed interval irrigation (every 7 days)
water_balance_fixed_interval <- calc_water_balance(
  et0 = et0_data,
  rainfall = rain_data,
  daily_kc_values = daily_kc_lettuce,
  root_depth = root_depth_val,
  theta_fc = theta_fc_val,
  theta_wp = theta_wp_val,
  depletion_factor = p_factor_val,
  irrigation_rule = 7
)
print(water_balance_fixed_interval$summary_depths$total_irrigation_applied)
print(water_balance_fixed_interval$summary_depths$irrigation_events_count)
}
\references{
Allen, R. G., Pereira, L. S., Raes, D., & Smith, M. (1998). Crop evapotranspiration - Guidelines for computing crop water requirements. FAO Irrigation and drainage paper 56.
}
